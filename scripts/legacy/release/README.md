# Legacy release tooling

These files preserve the former local build-and-publish implementation for
historical reference. They are unsupported, are not exercised by CI, and may
not run from this directory.

Do not use them for a current release. Follow the repository `RELEASE.md` and
promote only the immutable artifacts produced by the tagged GitHub Release.
