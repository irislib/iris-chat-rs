<UserControl x:Class="IrisChat.Views.CreateInviteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:chrome="clr-namespace:IrisChat.Chrome">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="{StaticResource Background}">
        <StackPanel Margin="32" MaxWidth="520" HorizontalAlignment="Center">
            <Border Background="{StaticResource Panel}"
                    CornerRadius="{StaticResource SectionRadius}"
                    Padding="22,20"
                    Margin="0,0,0,12">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Public invite"
                               Foreground="{StaticResource TextPrimary}"
                               FontSize="22"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,16" />

                    <TextBlock x:Name="LoadingText"
                               Text="Generating invite…"
                               Foreground="{StaticResource TextMuted}"
                               FontSize="12"
                               HorizontalAlignment="Center"
                               Visibility="Visible" />

                    <chrome:QrCodeView x:Name="InviteQr"
                                       Visibility="Collapsed" />

                    <Grid x:Name="ButtonsRow"
                          HorizontalAlignment="Center"
                          Margin="0,16,0,0"
                          Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0"
                                Style="{StaticResource SecondaryButton}"
                                Padding="22,10"
                                Click="OnCopy">
                            <TextBlock Text="Copy" />
                        </Button>
                        <Button Grid.Column="1"
                                Style="{StaticResource PrimaryButton}"
                                Padding="22,10"
                                Margin="10,0,0,0"
                                Click="OnNewInvite"
                                x:Name="NewInviteButton">
                            <TextBlock Text="New invite" />
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
