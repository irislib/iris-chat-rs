<UserControl x:Class="IrisChat.Views.DeviceRevokedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource Background}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="420" Margin="20">
            <TextBlock Text="This device was revoked"
                       Style="{StaticResource HeadingText}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,12" />
            <TextBlock Foreground="{StaticResource TextMuted}"
                       Text="The owner of this account revoked this device's access. To continue, you can sign back in or reset this device."
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Margin="0,0,0,20" />
            <Button Style="{StaticResource PrimaryButton}"
                    Click="OnAcknowledge"
                    Margin="0,0,0,10">
                <TextBlock Text="Acknowledge" />
            </Button>
            <Button Style="{StaticResource DangerButton}"
                    Click="OnReset">
                <TextBlock Text="Reset and sign out" />
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
