<UserControl x:Class="IrisChat.Views.RemoteSignerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:chrome="clr-namespace:IrisChat.Chrome">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32" MaxWidth="480" HorizontalAlignment="Center">
            <TextBlock Text="Signer app/device" Style="{StaticResource HeadingText}" Margin="0,0,0,20" />
            <TextBlock x:Name="StatusText" Style="{StaticResource MutedText}"
                       TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,20" />
            <ProgressBar x:Name="LoadingIndicator" IsIndeterminate="True" Width="120" Height="4" Margin="0,0,0,20" />
            <chrome:QrCodeView x:Name="SignerCode" Visibility="Collapsed" />
            <Button x:Name="CopyButton" Style="{StaticResource SecondaryButton}" Click="OnCopy" Margin="0,20,0,0">
                <TextBlock Text="Copy code" />
            </Button>
            <Button x:Name="RetryButton" Style="{StaticResource SecondaryButton}" Click="OnRetry" Visibility="Collapsed">
                <TextBlock Text="Try again" />
            </Button>
            <Button x:Name="ApprovalButton" Style="{StaticResource PrimaryButton}" Click="OnApproval" Margin="0,20,0,0" Visibility="Collapsed">
                <TextBlock Text="Open approval" />
            </Button>
            <Button x:Name="PasteButton" Style="{StaticResource SecondaryButton}" Click="OnPaste" Margin="0,20,0,0">
                <TextBlock Text="Paste signer link" />
            </Button>
            <StackPanel x:Name="LinkInputBlock" Visibility="Collapsed" Margin="0,20,0,0">
                <TextBlock Text="Signer link" Margin="0,0,0,6" />
                <TextBox x:Name="LinkInput" TextChanged="OnLinkChanged" />
                <Button x:Name="ConnectButton" Style="{StaticResource PrimaryButton}" Click="OnConnect" Margin="0,12,0,0" IsEnabled="False">
                    <TextBlock Text="Connect" />
                </Button>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
