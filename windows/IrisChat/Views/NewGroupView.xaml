<UserControl x:Class="IrisChat.Views.NewGroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32" MaxWidth="640" HorizontalAlignment="Center">
            <TextBlock Text="New group"
                       Style="{StaticResource HeadingText}"
                       Margin="0,0,0,12" />

            <TextBlock Text="Group name" Margin="0,0,0,4" />
            <TextBox x:Name="NameInput" />

            <TextBlock Text="Members" Margin="0,16,0,4" />
            <TextBlock Style="{StaticResource MutedText}"
                       Text="Add npubs (one per line)."
                       Margin="0,0,0,8" />
            <TextBox x:Name="MembersInput"
                     Height="120"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />

            <Button x:Name="CreateButton"
                    Style="{StaticResource PrimaryButton}"
                    Click="OnCreate"
                    Margin="0,16,0,0">
                <TextBlock Text="Create group" />
            </Button>
        </StackPanel>
    </ScrollViewer>
</UserControl>
