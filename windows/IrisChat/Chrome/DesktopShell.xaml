<UserControl x:Class="IrisChat.Chrome.DesktopShell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:chrome="clr-namespace:IrisChat.Chrome">
    <Grid Background="{StaticResource Background}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="352" />
            <ColumnDefinition Width="1" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar: profile button, search/new, chat list -->
        <Grid Grid.Column="0" Background="{StaticResource Background}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- top sidebar bar -->
            <Border Grid.Row="0"
                    Background="{StaticResource Toolbar}"
                    BorderBrush="{StaticResource Border}"
                    BorderThickness="0,0,0,1"
                    Padding="12,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Button x:Name="ProfileButton"
                            Grid.Column="0"
                            Style="{StaticResource IconButton}"
                            Padding="0"
                            Click="OnProfile">
                        <chrome:Avatar x:Name="ProfileAvatar" Size="34" />
                    </Button>

                    <TextBlock Grid.Column="1"
                               Text="Chats"
                               Foreground="{StaticResource TextPrimary}"
                               FontSize="16"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               Margin="10,0,0,0" />

                    <Button x:Name="NewChatButton"
                            Grid.Column="2"
                            Style="{StaticResource CompactPrimaryButton}"
                            Padding="10,4"
                            Click="OnNewChat">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="New" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <Border Grid.Row="1"
                    Background="{StaticResource Toolbar}"
                    Padding="10,8,10,6">
                <Grid>
                    <TextBox x:Name="SearchInput"
                             Padding="34,8,34,8"
                             TextChanged="OnSearchTextChanged"
                             KeyDown="OnSearchKeyDown" />
                    <TextBlock Text="&#xE721;"
                               FontFamily="Segoe MDL2 Assets"
                               Foreground="{StaticResource TextMuted}"
                               FontSize="14"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left"
                               Margin="12,0,0,0"
                               IsHitTestVisible="False" />
                    <TextBlock x:Name="SearchPlaceholder"
                               Text="Search"
                               Foreground="{StaticResource TextMuted}"
                               FontSize="14"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left"
                               Margin="34,0,0,0"
                               IsHitTestVisible="False" />
                    <Button x:Name="ClearSearchButton"
                            Style="{StaticResource IconButton}"
                            Width="30"
                            Height="30"
                            Padding="4"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Click="OnClearSearch"
                            Visibility="Collapsed"
                            ToolTip="Clear search">
                        <TextBlock Text="&#xE711;"
                                   FontFamily="Segoe MDL2 Assets"
                                   Foreground="{StaticResource TextMuted}"
                                   FontSize="12"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Button>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ChatRows" Padding="6,8" />
            </ScrollViewer>

            <!-- bottom sidebar bar: open invites / settings shortcut -->
            <Border Grid.Row="3"
                    Background="{StaticResource Toolbar}"
                    BorderBrush="{StaticResource Border}"
                    BorderThickness="0,1,0,0"
                    Padding="10,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0"
                            Style="{StaticResource GhostButton}"
                            Click="OnInvite"
                            Padding="6,4">
                        <TextBlock Text="Invite" />
                    </Button>
                    <Button Grid.Column="1"
                            Style="{StaticResource GhostButton}"
                            Click="OnNewGroup"
                            Padding="6,4">
                        <TextBlock Text="Group" />
                    </Button>
                    <Button Grid.Column="2"
                            Style="{StaticResource GhostButton}"
                            Click="OnSettings"
                            Padding="6,4">
                        <TextBlock Text="Settings" />
                    </Button>
                </Grid>
            </Border>
        </Grid>

        <!-- Sidebar/main divider -->
        <Border Grid.Column="1" Background="{StaticResource Border}" />

        <!-- Main content -->
        <ContentControl x:Name="MainHost" Grid.Column="2" />
    </Grid>
</UserControl>
